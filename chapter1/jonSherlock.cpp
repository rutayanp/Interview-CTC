#include <iostream>
#include <stdlib.h>

using namespace std;

int main()
{
    int *arr =(int *)malloc(sizeof(int) * N);
    int *new_start = arr;

    if ( k > 0)
        new_start  = &arr[N - k];

    for ()
}
